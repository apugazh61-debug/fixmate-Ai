"""
Curated, versioned evaluation corpus for FixMate AI benchmarking.

Contains 70+ verified code snippets:
- 15 Missing Imports (Python)
- 15 Syntax Errors (Python)
- 15 Undefined Variables / Typos (Python)
- 15 Out-of-Distribution Logic Bugs (Python)
- 10 JavaScript Snippets (Multi-language proof of generality)

Sources: Real StackOverflow questions (Creative Commons BY-SA) & programmatic mutation.
"""

from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True)
class CorpusItem:
    id: str
    language: str  # "python" | "javascript"
    error_type: str  # "missing_import" | "syntax_error" | "undefined_variable" | "out_of_distribution"
    broken_code: str
    ground_truth_fix: str
    description: str
    source: str  # "stackoverflow_mined" | "synthetic_mutation"


CORPUS: list[CorpusItem] = [
    # -------------------------------------------------------------------------
    # 1. MISSING IMPORTS (Python - 15 items)
    # -------------------------------------------------------------------------
    CorpusItem(
        id="py_mi_01",
        language="python",
        error_type="missing_import",
        broken_code="""def hypotenuse(a, b):\n    return math.sqrt(a**2 + b**2)\n\nprint(hypotenuse(3, 4))\n""",
        ground_truth_fix="""import math\n\ndef hypotenuse(a, b):\n    return math.sqrt(a**2 + b**2)\n\nprint(hypotenuse(3, 4))\n""",
        description="math.sqrt used without import math",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_mi_02",
        language="python",
        error_type="missing_import",
        broken_code="""def parse_payload(raw_str):\n    return json.loads(raw_str)\n\nprint(parse_payload('{"key": "val"}'))\n""",
        ground_truth_fix="""import json\n\ndef parse_payload(raw_str):\n    return json.loads(raw_str)\n\nprint(parse_payload('{"key": "val"}'))\n""",
        description="json.loads used without import json",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_mi_03",
        language="python",
        error_type="missing_import",
        broken_code="""def join_paths(folder, filename):\n    return os.path.join(folder, filename)\n\nprint(join_paths("data", "file.csv"))\n""",
        ground_truth_fix="""import os\n\ndef join_paths(folder, filename):\n    return os.path.join(folder, filename)\n\nprint(join_paths("data", "file.csv"))\n""",
        description="os.path.join used without import os",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_mi_04",
        language="python",
        error_type="missing_import",
        broken_code="""def roll_die():\n    return random.randint(1, 6)\n\nprint(roll_die())\n""",
        ground_truth_fix="""import random\n\ndef roll_die():\n    return random.randint(1, 6)\n\nprint(roll_die())\n""",
        description="random.randint used without import random",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_mi_05",
        language="python",
        error_type="missing_import",
        broken_code="""def check_pattern(text, pattern):\n    return re.search(pattern, text) is not None\n\nprint(check_pattern("abc123", r"\\d+"))\n""",
        ground_truth_fix="""import re\n\ndef check_pattern(text, pattern):\n    return re.search(pattern, text) is not None\n\nprint(check_pattern("abc123", r"\\d+"))\n""",
        description="re.search used without import re",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_mi_06",
        language="python",
        error_type="missing_import",
        broken_code="""def pause_seconds(s):\n    time.sleep(s)\n    return "Done"\n\nprint(pause_seconds(0.01))\n""",
        ground_truth_fix="""import time\n\ndef pause_seconds(s):\n    time.sleep(s)\n    return "Done"\n\nprint(pause_seconds(0.01))\n""",
        description="time.sleep used without import time",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_mi_07",
        language="python",
        error_type="missing_import",
        broken_code="""def get_today():\n    return datetime.date.today()\n\nprint(get_today())\n""",
        ground_truth_fix="""import datetime\n\ndef get_today():\n    return datetime.date.today()\n\nprint(get_today())\n""",
        description="datetime.date used without import datetime",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_mi_08",
        language="python",
        error_type="missing_import",
        broken_code="""def make_pairs(items):\n    return list(itertools.combinations(items, 2))\n\nprint(make_pairs([1, 2, 3]))\n""",
        ground_truth_fix="""import itertools\n\ndef make_pairs(items):\n    return list(itertools.combinations(items, 2))\n\nprint(make_pairs([1, 2, 3]))\n""",
        description="itertools.combinations used without import itertools",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_mi_09",
        language="python",
        error_type="missing_import",
        broken_code="""def count_words(words):\n    d = collections.defaultdict(int)\n    for w in words:\n        d[w] += 1\n    return dict(d)\n\nprint(count_words(["a", "b", "a"]))\n""",
        ground_truth_fix="""import collections\n\ndef count_words(words):\n    d = collections.defaultdict(int)\n    for w in words:\n        d[w] += 1\n    return dict(d)\n\nprint(count_words(["a", "b", "a"]))\n""",
        description="collections.defaultdict used without import collections",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_mi_10",
        language="python",
        error_type="missing_import",
        broken_code="""def multiply_all(nums):\n    return functools.reduce(lambda x, y: x * y, nums)\n\nprint(multiply_all([2, 3, 4]))\n""",
        ground_truth_fix="""import functools\n\ndef multiply_all(nums):\n    return functools.reduce(lambda x, y: x * y, nums)\n\nprint(multiply_all([2, 3, 4]))\n""",
        description="functools.reduce used without import functools",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_mi_11",
        language="python",
        error_type="missing_import",
        broken_code="""def get_file_ext(path_str):\n    p = pathlib.Path(path_str)\n    return p.suffix\n\nprint(get_file_ext("document.pdf"))\n""",
        ground_truth_fix="""import pathlib\n\ndef get_file_ext(path_str):\n    p = pathlib.Path(path_str)\n    return p.suffix\n\nprint(get_file_ext("document.pdf"))\n""",
        description="pathlib.Path used without import pathlib",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_mi_12",
        language="python",
        error_type="missing_import",
        broken_code="""def fetch_status(url):\n    return requests.get(url).status_code\n""",
        ground_truth_fix="""import requests\n\ndef fetch_status(url):\n    return requests.get(url).status_code\n""",
        description="requests.get used without import requests",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_mi_13",
        language="python",
        error_type="missing_import",
        broken_code="""def calc_circumference(r):\n    return 2 * math.pi * r\n\nprint(calc_circumference(5))\n""",
        ground_truth_fix="""import math\n\ndef calc_circumference(r):\n    return 2 * math.pi * r\n\nprint(calc_circumference(5))\n""",
        description="math.pi used without import math",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_mi_14",
        language="python",
        error_type="missing_import",
        broken_code="""def exit_program(code):\n    sys.exit(code)\n""",
        ground_truth_fix="""import sys\n\ndef exit_program(code):\n    sys.exit(code)\n""",
        description="sys.exit used without import sys",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_mi_15",
        language="python",
        error_type="missing_import",
        broken_code="""def shuffle_cards(cards):\n    random.shuffle(cards)\n    return cards\n\nprint(shuffle_cards([1, 2, 3, 4]))\n""",
        ground_truth_fix="""import random\n\ndef shuffle_cards(cards):\n    random.shuffle(cards)\n    return cards\n\nprint(shuffle_cards([1, 2, 3, 4]))\n""",
        description="random.shuffle used without import random",
        source="synthetic_mutation",
    ),

    # -------------------------------------------------------------------------
    # 2. SYNTAX ERRORS (Python - 15 items)
    # -------------------------------------------------------------------------
    CorpusItem(
        id="py_syn_01",
        language="python",
        error_type="syntax_error",
        broken_code="""def is_even(n)\n    return n % 2 == 0\n\nprint(is_even(4))\n""",
        ground_truth_fix="""def is_even(n):\n    return n % 2 == 0\n\nprint(is_even(4))\n""",
        description="Missing colon on def statement",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_syn_02",
        language="python",
        error_type="syntax_error",
        broken_code="""def check_val(x):\n    if x > 10\n        return "big"\n    return "small"\n\nprint(check_val(15))\n""",
        ground_truth_fix="""def check_val(x):\n    if x > 10:\n        return "big"\n    return "small"\n\nprint(check_val(15))\n""",
        description="Missing colon on if statement",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_syn_03",
        language="python",
        error_type="syntax_error",
        broken_code="""def print_items(items):\n    for item in items\n        print(item)\n\nprint_items([1, 2])\n""",
        ground_truth_fix="""def print_items(items):\n    for item in items:\n        print(item)\n\nprint_items([1, 2])\n""",
        description="Missing colon on for statement",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_syn_04",
        language="python",
        error_type="syntax_error",
        broken_code="""def countdown(n):\n    while n > 0\n        n -= 1\n    return n\n\nprint(countdown(3))\n""",
        ground_truth_fix="""def countdown(n):\n    while n > 0:\n        n -= 1\n    return n\n\nprint(countdown(3))\n""",
        description="Missing colon on while statement",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_syn_05",
        language="python",
        error_type="syntax_error",
        broken_code="""class Calculator\n    def add(self, a, b):\n        return a + b\n\nc = Calculator()\nprint(c.add(2, 3))\n""",
        ground_truth_fix="""class Calculator:\n    def add(self, a, b):\n        return a + b\n\nc = Calculator()\nprint(c.add(2, 3))\n""",
        description="Missing colon on class statement",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_syn_06",
        language="python",
        error_type="syntax_error",
        broken_code="""def read_content(filename):\n    with open(filename) as f\n        return f.read()\n""",
        ground_truth_fix="""def read_content(filename):\n    with open(filename) as f:\n        return f.read()\n""",
        description="Missing colon on with statement",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_syn_07",
        language="python",
        error_type="syntax_error",
        broken_code="""def safe_div(a, b):\n    try:\n        return a / b\n    except ZeroDivisionError\n        return 0\n\nprint(safe_div(1, 0))\n""",
        ground_truth_fix="""def safe_div(a, b):\n    try:\n        return a / b\n    except ZeroDivisionError:\n        return 0\n\nprint(safe_div(1, 0))\n""",
        description="Missing colon on except statement",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_syn_08",
        language="python",
        error_type="syntax_error",
        broken_code="""def classify(score):\n    if score >= 90:\n        return 'A'\n    elif score >= 80\n        return 'B'\n    return 'C'\n\nprint(classify(85))\n""",
        ground_truth_fix="""def classify(score):\n    if score >= 90:\n        return 'A'\n    elif score >= 80:\n        return 'B'\n    return 'C'\n\nprint(classify(85))\n""",
        description="Missing colon on elif statement",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_syn_09",
        language="python",
        error_type="syntax_error",
        broken_code="""def build_user(name, age)\n    return {"name": name, "age": age}\n\nprint(build_user("Sam", 25))\n""",
        ground_truth_fix="""def build_user(name, age):\n    return {"name": name, "age": age}\n\nprint(build_user("Sam", 25))\n""",
        description="Missing colon on function def header",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_syn_10",
        language="python",
        error_type="syntax_error",
        broken_code="""def match_key(k):\n    if k == "start"\n        return 1\n    else:\n        return 0\n\nprint(match_key("start"))\n""",
        ground_truth_fix="""def match_key(k):\n    if k == "start":\n        return 1\n    else:\n        return 0\n\nprint(match_key("start"))\n""",
        description="Missing colon on if branch preceding else",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_syn_11",
        language="python",
        error_type="syntax_error",
        broken_code="""def calculate(x, y)\n    if x < y\n        return y - x\n    return x - y\n\nprint(calculate(3, 7))\n""",
        ground_truth_fix="""def calculate(x, y):\n    if x < y:\n        return y - x\n    return x - y\n\nprint(calculate(3, 7))\n""",
        description="Multiple missing colons in def and if",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_syn_12",
        language="python",
        error_type="syntax_error",
        broken_code="""def iterate_matrix(grid):\n    for row in grid\n        for col in row:\n            pass\n    return True\n\nprint(iterate_matrix([[1], [2]]))\n""",
        ground_truth_fix="""def iterate_matrix(grid):\n    for row in grid:\n        for col in row:\n            pass\n    return True\n\nprint(iterate_matrix([[1], [2]]))\n""",
        description="Missing colon on outer loop",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_syn_13",
        language="python",
        error_type="syntax_error",
        broken_code="""def try_block():\n    try\n        x = 1\n    except:\n        x = 0\n    return x\n\nprint(try_block())\n""",
        ground_truth_fix="""def try_block():\n    try:\n        x = 1\n    except:\n        x = 0\n    return x\n\nprint(try_block())\n""",
        description="Missing colon on try block",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_syn_14",
        language="python",
        error_type="syntax_error",
        broken_code="""def handle_status(code):\n    if code == 200:\n        return "OK"\n    else\n        return "ERR"\n\nprint(handle_status(404))\n""",
        ground_truth_fix="""def handle_status(code):\n    if code == 200:\n        return "OK"\n    else:\n        return "ERR"\n\nprint(handle_status(404))\n""",
        description="Missing colon on else statement",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_syn_15",
        language="python",
        error_type="syntax_error",
        broken_code="""def square_all(nums):\n    res = []\n    for n in nums\n        res.append(n * n)\n    return res\n\nprint(square_all([1, 2, 3]))\n""",
        ground_truth_fix="""def square_all(nums):\n    res = []\n    for n in nums:\n        res.append(n * n)\n    return res\n\nprint(square_all([1, 2, 3]))\n""",
        description="Missing colon on loop in square_all",
        source="synthetic_mutation",
    ),

    # -------------------------------------------------------------------------
    # 3. UNDEFINED VARIABLES / TYPOS (Python - 15 items)
    # -------------------------------------------------------------------------
    CorpusItem(
        id="py_uv_01",
        language="python",
        error_type="undefined_variable",
        broken_code="""def get_length(elements):\n    return len(element)\n\nprint(get_length([1, 2, 3]))\n""",
        ground_truth_fix="""def get_length(elements):\n    return len(elements)\n\nprint(get_length([1, 2, 3]))\n""",
        description="Typo element instead of elements",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_uv_02",
        language="python",
        error_type="undefined_variable",
        broken_code="""def compute_tax(subtotal, tax_rate):\n    return subtotal * tax_rat\n\nprint(compute_tax(100, 0.05))\n""",
        ground_truth_fix="""def compute_tax(subtotal, tax_rate):\n    return subtotal * tax_rate\n\nprint(compute_tax(100, 0.05))\n""",
        description="Typo tax_rat instead of tax_rate",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_uv_03",
        language="python",
        error_type="undefined_variable",
        broken_code="""def filter_positive(numbers):\n    positives = []\n    for num in number:\n        if num > 0:\n            positives.append(num)\n    return positives\n\nprint(filter_positive([1, -2, 3]))\n""",
        ground_truth_fix="""def filter_positive(numbers):\n    positives = []\n    for num in numbers:\n        if num > 0:\n            positives.append(num)\n    return positives\n\nprint(filter_positive([1, -2, 3]))\n""",
        description="Typo number instead of numbers in loop",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_uv_04",
        language="python",
        error_type="undefined_variable",
        broken_code="""def greet_person(first_name, last_name):\n    full = f"{first_name} {last_name}"\n    return ful\n\nprint(greet_person("John", "Doe"))\n""",
        ground_truth_fix="""def greet_person(first_name, last_name):\n    full = f"{first_name} {last_name}"\n    return full\n\nprint(greet_person("John", "Doe"))\n""",
        description="Typo ful instead of full",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_uv_05",
        language="python",
        error_type="undefined_variable",
        broken_code="""def build_mapping(keys, values):\n    mapping = {}\n    for k, v in zip(keys, values):\n        mapping[k] = v\n    return maping\n\nprint(build_mapping(['a'], [1]))\n""",
        ground_truth_fix="""def build_mapping(keys, values):\n    mapping = {}\n    for k, v in zip(keys, values):\n        mapping[k] = v\n    return mapping\n\nprint(build_mapping(['a'], [1]))\n""",
        description="Typo maping instead of mapping",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_uv_06",
        language="python",
        error_type="undefined_variable",
        broken_code="""def average(records):\n    total_sum = sum(records)\n    return total_sum / len(record)\n\nprint(average([10, 20, 30]))\n""",
        ground_truth_fix="""def average(records):\n    total_sum = sum(records)\n    return total_sum / len(records)\n\nprint(average([10, 20, 30]))\n""",
        description="Typo record instead of records in len()",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_uv_07",
        language="python",
        error_type="undefined_variable",
        broken_code="""def format_address(city, zip_code):\n    return f"{city}, {zipcode}"\n\nprint(format_address("Austin", "78701"))\n""",
        ground_truth_fix="""def format_address(city, zip_code):\n    return f"{city}, {zip_code}"\n\nprint(format_address("Austin", "78701"))\n""",
        description="Typo zipcode instead of zip_code in f-string",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_uv_08",
        language="python",
        error_type="undefined_variable",
        broken_code="""def find_maximum(values_list):\n    max_val = values_list[0]\n    for v in values_list:\n        if v > max_val:\n            max_val = v\n    return maxval\n\nprint(find_maximum([4, 1, 9]))\n""",
        ground_truth_fix="""def find_maximum(values_list):\n    max_val = values_list[0]\n    for v in values_list:\n        if v > max_val:\n            max_val = v\n    return max_val\n\nprint(find_maximum([4, 1, 9]))\n""",
        description="Typo maxval instead of max_val",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_uv_09",
        language="python",
        error_type="undefined_variable",
        broken_code="""def concat_strings(str_a, str_b):\n    return stra + str_b\n\nprint(concat_strings("hello", "world"))\n""",
        ground_truth_fix="""def concat_strings(str_a, str_b):\n    return str_a + str_b\n\nprint(concat_strings("hello", "world"))\n""",
        description="Typo stra instead of str_a",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_uv_10",
        language="python",
        error_type="undefined_variable",
        broken_code="""def scale_vector(vector, multiplier):\n    return [x * multipler for x in vector]\n\nprint(scale_vector([1, 2], 3))\n""",
        ground_truth_fix="""def scale_vector(vector, multiplier):\n    return [x * multiplier for x in vector]\n\nprint(scale_vector([1, 2], 3))\n""",
        description="Typo multipler instead of multiplier",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_uv_11",
        language="python",
        error_type="undefined_variable",
        broken_code="""def square_root_sum(numbers):\n    acc = 0\n    for n in numbers:\n        acc += n ** 0.5\n    return ac\n\nprint(square_root_sum([4, 9]))\n""",
        ground_truth_fix="""def square_root_sum(numbers):\n    acc = 0\n    for n in numbers:\n        acc += n ** 0.5\n    return acc\n\nprint(square_root_sum([4, 9]))\n""",
        description="Typo ac instead of acc",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_uv_12",
        language="python",
        error_type="undefined_variable",
        broken_code="""def get_dimensions(width, height):\n    area = width * height\n    return widht, height, area\n\nprint(get_dimensions(10, 20))\n""",
        ground_truth_fix="""def get_dimensions(width, height):\n    area = width * height\n    return width, height, area\n\nprint(get_dimensions(10, 20))\n""",
        description="Typo widht instead of width",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_uv_13",
        language="python",
        error_type="undefined_variable",
        broken_code="""def is_adult(person_age):\n    return personage >= 18\n\nprint(is_adult(20))\n""",
        ground_truth_fix="""def is_adult(person_age):\n    return person_age >= 18\n\nprint(is_adult(20))\n""",
        description="Typo personage instead of person_age",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_uv_14",
        language="python",
        error_type="undefined_variable",
        broken_code="""def build_query(params):\n    clause = " AND ".join(params)\n    return f"SELECT * WHERE {clauses}"\n\nprint(build_query(["a=1"]))\n""",
        ground_truth_fix="""def build_query(params):\n    clause = " AND ".join(params)\n    return f"SELECT * WHERE {clause}"\n\nprint(build_query(["a=1"]))\n""",
        description="Typo clauses instead of clause",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_uv_15",
        language="python",
        error_type="undefined_variable",
        broken_code="""def clean_tokens(tokens):\n    cleaned = []\n    for t in token:\n        cleaned.append(t.strip())\n    return cleaned\n\nprint(clean_tokens([" a ", " b "]))\n""",
        ground_truth_fix="""def clean_tokens(tokens):\n    cleaned = []\n    for t in tokens:\n        cleaned.append(t.strip())\n    return cleaned\n\nprint(clean_tokens([" a ", " b "]))\n""",
        description="Typo token instead of tokens in for loop",
        source="synthetic_mutation",
    ),

    # -------------------------------------------------------------------------
    # 4. OUT-OF-DISTRIBUTION / COMPLEX LOGIC (Python - 15 items)
    # -------------------------------------------------------------------------
    CorpusItem(
        id="py_ood_01",
        language="python",
        error_type="out_of_distribution",
        broken_code="""def get_last_element(arr):\n    # Off-by-one index error\n    return arr[len(arr)]\n""",
        ground_truth_fix="""def get_last_element(arr):\n    return arr[-1] if arr else None\n""",
        description="IndexError: accessing arr[len(arr)] instead of arr[-1]",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_ood_02",
        language="python",
        error_type="out_of_distribution",
        broken_code="""def append_to_list(val, my_list=[]):\n    # Mutable default argument bug\n    my_list.append(val)\n    return my_list\n""",
        ground_truth_fix="""def append_to_list(val, my_list=None):\n    if my_list is None:\n        my_list = []\n    my_list.append(val)\n    return my_list\n""",
        description="Python mutable default argument retention bug",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_ood_03",
        language="python",
        error_type="out_of_distribution",
        broken_code="""def divide_ratio(numerator, denominator):\n    return numerator / denominator\n\n# Crashes on ZeroDivisionError when denominator is 0\n""",
        ground_truth_fix="""def divide_ratio(numerator, denominator):\n    if denominator == 0:\n        return 0.0\n    return numerator / denominator\n""",
        description="Unhandled ZeroDivisionError in calculation",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_ood_04",
        language="python",
        error_type="out_of_distribution",
        broken_code="""def add_strings_and_number(a, b):\n    # TypeError: unsupported operand type(s) for +: 'int' and 'str'\n    return a + b\n""",
        ground_truth_fix="""def add_strings_and_number(a, b):\n    return int(a) + int(b)\n""",
        description="Implicit type conversion mismatch adding str and int",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_ood_05",
        language="python",
        error_type="out_of_distribution",
        broken_code="""def get_nested(d, key1, key2):\n    # KeyError when key1 not in d\n    return d[key1][key2]\n""",
        ground_truth_fix="""def get_nested(d, key1, key2):\n    return d.get(key1, {}).get(key2, None)\n""",
        description="Unsafe dict access raising KeyError",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_ood_06",
        language="python",
        error_type="out_of_distribution",
        broken_code="""def binary_search(arr, target):\n    low = 0\n    high = len(arr)\n    while low <= high:\n        mid = (low + high) // 2\n        if arr[mid] == target:\n            return mid\n        elif arr[mid] < target:\n            low = mid + 1\n        else:\n            high = mid - 1\n    return -1\n""",
        ground_truth_fix="""def binary_search(arr, target):\n    low = 0\n    high = len(arr) - 1\n    while low <= high:\n        mid = (low + high) // 2\n        if arr[mid] == target:\n            return mid\n        elif arr[mid] < target:\n            low = mid + 1\n        else:\n            high = mid - 1\n    return -1\n""",
        description="Off-by-one initialization high = len(arr) instead of len(arr) - 1",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_ood_07",
        language="python",
        error_type="out_of_distribution",
        broken_code="""def remove_all(items, val):\n    # Modifying list while iterating bug\n    for item in items:\n        if item == val:\n            items.remove(item)\n    return items\n""",
        ground_truth_fix="""def remove_all(items, val):\n    return [item for item in items if item != val]\n""",
        description="Mutating list during iteration causes skipped elements",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_ood_08",
        language="python",
        error_type="out_of_distribution",
        broken_code="""def sort_dict_by_value(d):\n    # Attempting to call .sort() on a dict\n    d.sort()\n    return d\n""",
        ground_truth_fix="""def sort_dict_by_value(d):\n    return dict(sorted(d.items(), key=lambda item: item[1]))\n""",
        description="AttributeError: dict has no attribute sort",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_ood_09",
        language="python",
        error_type="out_of_distribution",
        broken_code="""def is_palindrome(s):\n    # Wrong slice step\n    return s == s[0:-1]\n""",
        ground_truth_fix="""def is_palindrome(s):\n    return s == s[::-1]\n""",
        description="Incorrect slice logic for palindrome reversal",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_ood_09b",
        language="python",
        error_type="out_of_distribution",
        broken_code="""def get_first_or_default(lst, default_val):\n    if lst:\n        return lst[1]\n    return default_val\n""",
        ground_truth_fix="""def get_first_or_default(lst, default_val):\n    if lst:\n        return lst[0]\n    return default_val\n""",
        description="Off-by-one returning 1st index instead of 0th",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_ood_10",
        language="python",
        error_type="out_of_distribution",
        broken_code="""def calculate_average(nums):\n    # Unhandled empty list division by zero\n    return sum(nums) / len(nums)\n""",
        ground_truth_fix="""def calculate_average(nums):\n    if not nums:\n        return 0.0\n    return sum(nums) / len(nums)\n""",
        description="ZeroDivisionError when nums is empty",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="py_ood_11",
        language="python",
        error_type="out_of_distribution",
        broken_code="""def flatten_list(nested):\n    res = []\n    for sub in nested:\n        res.append(sub)\n    return res\n""",
        ground_truth_fix="""def flatten_list(nested):\n    res = []\n    for sub in nested:\n        if isinstance(sub, list):\n            res.extend(sub)\n        else:\n            res.append(sub)\n    return res\n""",
        description="append instead of extend for list flattening",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_ood_12",
        language="python",
        error_type="out_of_distribution",
        broken_code="""def is_subset(a, b):\n    # Misleading condition: checking identity instead of subset\n    return a is b\n""",
        ground_truth_fix="""def is_subset(a, b):\n    return set(a).issubset(set(b))\n""",
        description="Identity comparison 'is' instead of set containment",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_ood_13",
        language="python",
        error_type="out_of_distribution",
        broken_code="""def find_common_elements(l1, l2):\n    # Returns boolean instead of elements list\n    return l1 in l2\n""",
        ground_truth_fix="""def find_common_elements(l1, l2):\n    return list(set(l1) & set(l2))\n""",
        description="Invalid containment check on lists",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="py_ood_14",
        language="python",
        error_type="out_of_distribution",
        broken_code="""def parse_int_safe(val):\n    return int(val)\n""",
        ground_truth_fix="""def parse_int_safe(val, default=0):\n    try:\n        return int(val)\n    except (ValueError, TypeError):\n        return default\n""",
        description="Unhandled ValueError on non-numeric string parse",
        source="stackoverflow_mined",
    ),

    # -------------------------------------------------------------------------
    # 5. JAVASCRIPT SNIPPETS (Multi-Language Generality - 10 items)
    # -------------------------------------------------------------------------
    CorpusItem(
        id="js_mi_01",
        language="javascript",
        error_type="missing_import",
        broken_code="""function readConfigFile(filePath) {\n    return fs.readFileSync(filePath, 'utf8');\n}\n""",
        ground_truth_fix="""const fs = require('fs');\n\nfunction readConfigFile(filePath) {\n    return fs.readFileSync(filePath, 'utf8');\n}\n""",
        description="Node fs used without require/import",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="js_mi_02",
        language="javascript",
        error_type="missing_import",
        broken_code="""function getFileExtension(filename) {\n    return path.extname(filename);\n}\n""",
        ground_truth_fix="""const path = require('path');\n\nfunction getFileExtension(filename) {\n    return path.extname(filename);\n}\n""",
        description="Node path used without require/import",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="js_mi_03",
        language="javascript",
        error_type="missing_import",
        broken_code="""function generateHash(data) {\n    return crypto.createHash('sha256').update(data).digest('hex');\n}\n""",
        ground_truth_fix="""const crypto = require('crypto');\n\nfunction generateHash(data) {\n    return crypto.createHash('sha256').update(data).digest('hex');\n}\n""",
        description="Node crypto used without require/import",
        source="stackoverflow_mined",
    ),
    CorpusItem(
        id="js_syn_01",
        language="javascript",
        error_type="syntax_error",
        broken_code="""function calculateTotal(items) {\n    let total = 0;\n    for (const item of items {\n        total += item;\n    }\n    return total;\n}\n""",
        ground_truth_fix="""function calculateTotal(items) {\n    let total = 0;\n    for (const item of items) {\n        total += item;\n    }\n    return total;\n}\n""",
        description="Missing closing parenthesis on for...of loop",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="js_syn_02",
        language="javascript",
        error_type="syntax_error",
        broken_code="""function checkStatus(code) {\n    if (code === 200 {\n        return "OK";\n    }\n    return "ERROR";\n}\n""",
        ground_truth_fix="""function checkStatus(code) {\n    if (code === 200) {\n        return "OK";\n    }\n    return "ERROR";\n}\n""",
        description="Missing closing parenthesis on if condition",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="js_syn_03",
        language="javascript",
        error_type="syntax_error",
        broken_code="""function makeUser(name, age) {\n    return {\n        name: name,\n        age: age\n;\n}\n""",
        ground_truth_fix="""function makeUser(name, age) {\n    return {\n        name: name,\n        age: age\n    };\n}\n""",
        description="Unbalanced object bracket in return statement",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="js_uv_01",
        language="javascript",
        error_type="undefined_variable",
        broken_code="""function processUsers(usersList) {\n    const active = [];\n    for (const u of userList) {\n        if (u.isActive) active.push(u);\n    }\n    return active;\n}\n""",
        ground_truth_fix="""function processUsers(usersList) {\n    const active = [];\n    for (const u of usersList) {\n        if (u.isActive) active.push(u);\n    }\n    return active;\n}\n""",
        description="Typo userList instead of usersList",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="js_uv_02",
        language="javascript",
        error_type="undefined_variable",
        broken_code="""function getFullName(firstName, lastName) {\n    const full = `${firstName} ${lastName}`;\n    return ful;\n}\n""",
        ground_truth_fix="""function getFullName(firstName, lastName) {\n    const full = `${firstName} ${lastName}`;\n    return full;\n}\n""",
        description="Typo ful instead of full in return statement",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="js_uv_03",
        language="javascript",
        error_type="undefined_variable",
        broken_code="""function calculateDiscount(price, discountRate) {\n    return price * (1 - discountRat);\n}\n""",
        ground_truth_fix="""function calculateDiscount(price, discountRate) {\n    return price * (1 - discountRate);\n}\n""",
        description="Typo discountRat instead of discountRate",
        source="synthetic_mutation",
    ),
    CorpusItem(
        id="js_ood_01",
        language="javascript",
        error_type="out_of_distribution",
        broken_code="""function sumArray(arr) {\n    return arr.reduce((a, b) => a + b);\n}\n""",
        ground_truth_fix="""function sumArray(arr) {\n    return (arr || []).reduce((a, b) => a + b, 0);\n}\n""",
        description="TypeError on empty array without initial value in reduce",
        source="stackoverflow_mined",
    ),
]


def get_corpus(language: str | None = None) -> list[CorpusItem]:
    """Retrieve corpus items optionally filtered by language."""
    if language:
        return [item for item in CORPUS if item.language == language]
    return list(CORPUS)
